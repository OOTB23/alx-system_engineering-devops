#include "main.h"

/**
 * print_numbers - writes the alphabet
 *
 * Return: nothing.
 *
 */
void print_numbers(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		_putchar(0 + 48);
	}
	_putchar('\n');
}
