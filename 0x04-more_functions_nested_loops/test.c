#include "main.h"
#include <stdio.h>
/**
 * print_numbers - writes the number 0-9
 *
 * Return: nothing.
 *
 */
int main(void)
{
/**void print_numbers(void)
{
*/
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		putchar(0 + 48);
	}
	putchar('\n');

return (0);
}
