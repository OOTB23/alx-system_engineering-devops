#include <stdio.h>

/**
 * main - entery point
 *
 * describtion: function that print hexa numbers
 *
 * Return: (0) Always success
 */

int main(void)
{
	int c;

	for (c = 48 ; c <= 57 ; c++)
	{
		putchar(c);
	}
	for (c = 97 ; c <= 102 ; c++)
	{
		putchar(c);
	}
	putchar('\n');

return (0);

}
