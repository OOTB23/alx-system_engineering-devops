#include <stdio.h>

/**
 * main - entery point
 *
 * describtion: count from 0 to 10
 *
 * Return: (0) Always success
 */

int main(void)
{
	int c;

	for (c = 0 ; c <= 9 ; c++)
	{
		printf("%d", c);
	}

	putchar('\n');

return (0);

}
