#include "main.h"
#include <string.h>
/**
 * _strlen - update whatever value to 98
 *
 * @s: pointer to string
 *
 * Return: nothing.
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}
